class Product {
  String id, name, description, type, budget, quantity, date;
  Product({
    this.id,
    this.name,
    this.description,
    this.type,
    this.budget,
    this.quantity,
    this.date,
  });
}
